fingerprintjs2-rails
=============

### Installation

Add this to your Gemfile

`gem 'fingerprintjs2-rails'`

and

`bundle install`

After that you can add the file to sprockets:

`//= require fingerprint2`
